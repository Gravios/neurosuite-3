<?xml version='1.0'?>
<parameter>
 <program>
  <name>ndm_extractspikes</name>
  <parameters>
   <parameter>
    <name>inputExtension</name>
    <value>fil</value>
    <status>Optional</status>
   </parameter>
  </parameters>
  <help>Extract spike waveforms at previously-detected spike times (this creates .spk files).

Spike times are produced by ndm_detectspikes and stored in the shared .res files. ndm_extractspikes re-extracts a waveform for every one of those spikes and writes .spk; it does not detect, and errors if no .res exists yet. Because the spike times are method-independent, the extraction engine (method: standard, sdiff, or stderiv) may differ from the one used for detection, and ndm_extractspikes may be run several times with different methods to produce waveforms in different domains, each reusing the same .res.

The engine (method) and, for sdiff/stderiv, the spatial-derivative order (methodOrder) are set in the session's ndm_extractspikes program node; they are not declared here. Extraction needs no threshold or noise-window parameters — those belong to ndm_detectspikes.

PARAMETERS

# inputExtension

Extension of the data file read for extraction, without the leading dot (default
fil, i.e. &lt;session&gt;.fil). Set it to an alternative, externally-processed source
(e.g. car.fil or filtered.dat) to extract from that file instead. The source
must be high-pass filtered like the usual .fil; ndm_extractspikes does not filter it.

METHOD TOKENS

The method may be qualified so the transform is stated in the artifact name rather
than inferred: stderiv_S&lt;order&gt; runs the plain spatial derivative (orders 1-3) and
leaves any per-group sdiffPairs unused, while stderiv_C&lt;order&gt; applies the session's
sdiffPairs pattern -- order 4 for a single-partner pattern, order 5 for a
reference-set pattern (one written with '+'). Outputs are stamped with the full
token, e.g. &lt;session&gt;.spk.stderiv_C4.&lt;group&gt;.

Orders 4 and 5 are derived from the pattern itself and cannot be requested through
methodOrder; a plain "stderiv" keeps the historical behaviour of applying whatever
sdiffPairs the groups define.
</help>
 </program>
</parameter>
