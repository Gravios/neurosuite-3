<?xml version='1.0'?>
<parameter>
 <program>
  <name>ndm_pca</name>
  <parameters>
   <parameter>
    <name>before</name>
    <value></value>
    <status>Optional</status>
   </parameter>
   <parameter>
    <name>after</name>
    <value></value>
    <status>Optional</status>
   </parameter>
   <parameter>
    <name>extra</name>
    <value>true</value>
    <status>Optional</status>
   </parameter>
   <parameter>
    <name>varimax</name>
    <value>false</value>
    <status>Optional</status>
   </parameter>
   <parameter>
    <name>varimaxMaxIter</name>
    <value></value>
    <status>Optional</status>
   </parameter>
   <parameter>
    <name>varimaxTol</name>
    <value></value>
    <status>Optional</status>
   </parameter>
  </parameters>
  <help>Compute principal component analysis (PCA).

PARAMETERS

All mandatory parameters are defined in the 'Acquisition System' and 'Spike Groups' tab.

OPTIONAL PARAMETERS

# before

Number of samples before the peak to use for the PCA. This can be either a space-separated list of numbers (one per electrode group), or a single common number for all groups.

# after

Number of samples after the peak to use for the PCA. This can be either a space-separated list of numbers (one per electrode group), or a single common number for all groups.

# extra

Whether extra features (spike peak value) should be included in the output files.

# varimax

Apply a Varimax rotation to each channel's kept PCA components before projection (default false). Total explained variance is unchanged, but each rotated component tends to localise on a few waveform samples, which can make cluster structure easier to read during manual sorting. The rotation is baked into the saved .pca basis, so all downstream tools (including Klusters reprojection) follow it automatically. Applies to standard and stderiv methods alike.

# varimaxMaxIter

Maximum number of Varimax rotation sweeps (default 30). Only used when varimax is enabled.

# varimaxTol

Relative change in the Varimax criterion at which the rotation is considered converged (default 1e-6). Only used when varimax is enabled.
METHOD TOKENS

The method may be qualified so the transform is stated in the artifact name rather
than inferred: stderiv_S&lt;order&gt; runs the plain spatial derivative (orders 1-3) and
leaves any per-group sdiffPairs unused, while stderiv_C&lt;order&gt; applies the session's
sdiffPairs pattern -- order 4 for a single-partner pattern, order 5 for a
reference-set pattern (one written with '+'). Outputs are stamped with the full
token, e.g. &lt;session&gt;.spk.stderiv_C4.&lt;group&gt;.

Orders 4 and 5 are derived from the pattern itself and cannot be requested through
methodOrder; a plain "stderiv" keeps the historical behaviour of applying whatever
sdiffPairs the groups define.
</help>
 </program>
</parameter>
