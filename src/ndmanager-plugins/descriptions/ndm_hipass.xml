<?xml version='1.0'?>
<parameter>
 <program>
  <name>ndm_hipass</name>
  <parameters>
   <parameter>
    <name>windowHalfLength</name>
    <value>10</value>
    <status>Mandatory</status>
   </parameter>
   <parameter>
    <name>chunkSize</name>
    <value>134217728</value>
    <status>Optional</status>
   </parameter>
   <parameter>
    <name>inputExtension</name>
    <value>dat</value>
    <status>Optional</status>
   </parameter>
  </parameters>
  <help>High-pass filter a .dat file (required for spike extraction).

The program uses a median-based (non-linear) filter to minimize spike waveform distortion.

PARAMETERS

# windowHalfWidth

Determines the cutoff frequency

# chunkSize

Number of bytes of input processed per pass (default 134217728 = 128 MiB).
Reduce it if memory is tight. Capped internally at 512 MiB, so a larger value
is clamped rather than honoured.

# inputExtension

Extension of the data file read for filtering, without the leading dot (default
dat). Point it at an alternative, externally-processed source (e.g. car.dat) to
filter that file instead of the raw recording.
</help>
 </program>
</parameter>
