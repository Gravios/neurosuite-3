find_package(LibXml2 REQUIRED)
add_executable(xpathReader xpathReader.c)
target_link_libraries(xpathReader PRIVATE LibXml2::LibXml2)
install(TARGETS xpathReader DESTINATION "${CMAKE_INSTALL_BINDIR}")
add_manpage(xpathReader)
